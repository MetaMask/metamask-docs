Returns the value from a storage position at a given address.

:::info

Currently, the opBNB network service supports only near head requests (the latest 128 blocks).
Archive requests aren't available at this time.

:::

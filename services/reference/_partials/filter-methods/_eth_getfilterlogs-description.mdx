import CreditCost from '@site/src/components/CreditCost/CreditCostPrice.js';

Returns an array of all logs matching the filter with the given filter ID. <CreditCost network="evm" method="eth_getFilterLogs" />

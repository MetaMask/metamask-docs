import CreditCost from '@site/src/components/CreditCost/CreditCostPrice.js';

Polling method for a filter, which returns an array of logs which occurred since the last poll. <CreditCost network="evm" method="eth_getFilterChanges" />

Filter must be created by calling either [`eth_newFilter`](../eth_newfilter) or [`eth_newBlockFilter`](../eth_newblockfilter).

import CreditCost from '@site/src/components/CreditCost/CreditCostPrice.js';

Creates a new subscription for particular events. The node returns a subscription ID. For each event
that matches the subscription, a notification with relevant data is sent together with the subscription ID. <CreditCost network="evm" method="eth_subscribe" />

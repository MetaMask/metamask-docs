import CreditCost from '@site/src/components/CreditCost/CreditCostPrice.js'

Submits a user operation to be included onchain and returns the user operation hash if successful (or queued).
If the operation is not successful, it will return an error. <CreditCost network="bundler" method="eth_sendUserOperation" />

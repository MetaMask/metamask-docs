import CreditCost from '@site/src/components/CreditCost/CreditCostPrice.js';

Executes a list of transaction bundles without creating transactions on the blockchain. This is useful for simulating transactions and reviewing outcomes before submitting them. <CreditCost network="megaeth" method="eth_callMany" />

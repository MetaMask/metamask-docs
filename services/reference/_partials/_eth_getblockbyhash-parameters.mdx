- `hash`: (string) [_Required_] A string representing the hash (32 bytes) of a block.
- `transaction details flag`: (boolean) [_Required_] If set to `true`, returns the full transaction objects, if `false` returns only the hashes of the transactions.

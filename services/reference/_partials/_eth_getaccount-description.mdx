import CreditCost from '@site/src/components/CreditCost/CreditCostPrice.js';

Returns account details (code hash, storage root, balance, nonce) for a given address at a specified block. <CreditCost network="megaeth" method="eth_getAccount" />

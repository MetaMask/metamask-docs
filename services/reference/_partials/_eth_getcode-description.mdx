import CreditCost from '@site/src/components/CreditCost/CreditCostPrice.js';

Returns the compiled byte code of a smart contract, if any, at a given address. <CreditCost network="evm" method="eth_getCode" />

import CreditCost from '@site/src/components/CreditCost/CreditCostPrice.js';

Returns `true` if client is actively listening for network connections. <CreditCost network="evm" method="net_listening" />

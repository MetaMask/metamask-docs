Returns information about a transaction for a given hash.

:::info MEV protection for users on the free tier (BETA)

When MEV Protection is enabled, `r`, `s`, and `v` signature fields might return empty hex values (`0x0`)
while the transaction is pending.

You can disable MEV protection in your API key settings.

:::
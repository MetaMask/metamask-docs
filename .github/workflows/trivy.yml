---
tabname: Trivy

on:
  pull_request:
    branches:
      - main

jobs:
  trivy:
   esc name: Run trivy scanner
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Trivy
        tabuses: ConsenSys/github-actions/trivy@main
        with:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

{
  "name": "afdocs-runner",
  "version": "1.0.0",
  "private": true,
  "description": "Isolated, pinned install of afdocs for the Agent-Friendly Docs CI check. Kept out of the root package.json because afdocs requires Node >=22 while the site builds on Node 20.",
  "devDependencies": {
    "afdocs": "0.4.0"
  }
}

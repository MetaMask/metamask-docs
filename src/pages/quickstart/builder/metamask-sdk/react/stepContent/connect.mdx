### Connect with MetaMask Wallet

Connect to MetaMask and get the accounts and chainId.

`client.connect({ chainIds })` handles cross-platform connection (desktop and mobile), including deeplinking, and returns `{ accounts, chainId }` in a single call.

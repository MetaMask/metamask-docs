### Initialize the MetaMask SDK

Initialize the MetaMask SDK with these options:

- `dappMetadata` - Display your dapp's name, URL, and icon URL during connection.
- `infuraAPIKey` - Enables read-only RPC and load‑balancing.

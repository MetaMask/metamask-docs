### Sign out the user

Use the [`logout`](/embedded-wallets/sdk/js/usage/logout/) function to
sign out the user. This also clears the session information from the browser's local storage.

:::info

The Embedded Wallets Angular SDK provides additional functions.
See the [functions reference](/embedded-wallets/sdk/js/usage) for more information.

:::

### Sign in the user

Use the [`login`](/embedded-wallets/sdk/ios/usage/login) function to access
the sign-in functionality.
You can trigger this function from a button or other user action.

After a successful sign-in, the SDK stores the session state internally, and the user is redirected
back to your app.

### Set up the Web3Auth provider

Register `VueQueryPlugin` in `main.ts`, then wrap your app in `App.vue` with `Web3AuthProvider` and
`WagmiProvider` from `@web3auth/modal/vue/wagmi`. Use the configuration from the previous step.

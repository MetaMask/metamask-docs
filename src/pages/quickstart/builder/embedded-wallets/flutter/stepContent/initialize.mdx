### Initialize Web3Auth

Initialize a `Web3Auth` instance with your project's client ID and your target network
(this quickstart uses `sapphire_mainnet`).

:::info

See the [Embedded Wallets Flutter SDK documentation](/embedded-wallets/sdk/flutter) for more information.

:::

### Set up the Web3Auth provider

In `main.tsx`, wrap your app with `Web3AuthProvider`, then `QueryClientProvider`, then
`WagmiProvider` from `@web3auth/modal/react/wagmi`. Import `Web3AuthProvider` from
`@web3auth/modal/react` and pass the configuration from the previous step.

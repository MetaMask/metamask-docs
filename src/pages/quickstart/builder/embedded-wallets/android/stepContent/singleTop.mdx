### Set sign-in activity to `singleTop` launch mode

Ensure your sign-in activity `launchMode` is set to `singleTop` in the `AndroidManifest.xml` file.

:::tip

Copy the `activity_main.xml` file from this quickstart to your project for easier integration.

:::

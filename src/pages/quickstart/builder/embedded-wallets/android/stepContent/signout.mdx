### Sign out the user

Use the [`logout`](/embedded-wallets/sdk/android/usage/logout) function to
sign out the user. This also clears the session information from the browser's local storage.

:::info

The Embedded Wallets Android SDK provides additional functions.
See the [functions reference](/embedded-wallets/sdk/android/usage) for more information.

:::

### Sign out the user

Use the [`logout`](/embedded-wallets/sdk/react-native/usage/logout) function to
sign out the user. This also clears the session information from the browser's local storage.

:::info

The Embedded Wallets React Native SDK provides additional functions.
See the [functions reference](/embedded-wallets/sdk/react-native/usage) for more information.

:::

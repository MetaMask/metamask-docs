import CodeBlock from '@theme/CodeBlock'

{!props.only && <h3>{props.example.title ? props.example.title : `Example ${props.index}`}</h3>}

<CodeBlock language={props.example.language}>
  {props.example.content}
</CodeBlock>


import Parameter from '../Parameters/Parameter/index.mdx';

## Returns

{Array.isArray(props.method.result) ?
  props.method.result.map((parameter, index) => (
    <Parameter key={index} parameter={parameter} />
  ))
  : <Parameter parameter={props.method.result} />
}

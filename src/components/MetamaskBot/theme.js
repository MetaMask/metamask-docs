import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  palette: {
    type: "light",
    primary: {
      main: "rgb(3, 125, 214)", // Blue
    },
    secondary: {
      main: "rgb(246, 133, 27)", // Orange
    },
  },
});
